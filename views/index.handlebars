<body>
  <div class="container">
    <div class="row">
      <div class="col s-12">
        <h1><a href="/">Metal News</a></h1>
      </div>
    </div>
    <div class="row mt-4">
      <div class="col-lg-6 offset-lg-3">
        <a href="/articles"class="btn btn-primary btn-lg disabled" role="button" aria-pressed="true">Articles</a>
      </div>
    </div>

    <div class="row mt-4">
      <div class="col-lg-6 offset-lg-3">
        <ul class="collection">
                     
            {{#each articles}}
                {{> display/category}}
                {{> list-items/post}}  
           {{/each}}
        </ul>
      </div>
    </div>  
   </div>    
  </div>
</body>